[project]
name = "revisao"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "GbrNovo", email = "gabrielnovobrandao@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
]

[project.scripts]
revisao = "revisao:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
